gcd a b = (\r -> if r==0 then b else gcd b r) $ mod a b
Скобки:
f(a,b)
(f a) b)

Работает:
True || undefined
False || undefined
Хаскель сугубо говоря нестрогий,  Т.е. пока не потребуется значение, оно не будет посчитано

Списки и кортежи
Упорядоченная, может быть пустая последовательность элементов
1:2:[]П
Паттерн матчинг: |[x,y,z]  - список сиз трех эktментов, первый - X , второй y, третий z
|[x:xs] - на голову и хвост
|f[x,y,_]| x>y

Написать функцию divide которая по n вернет список всех делителей - переберать до квадратного корня,
включить как положительные, так и отрицательные числа

Функция конкатенации записать.
Список простых чисел - нужно посчитать, что число делителей от н равно двум. Функция одного - двух аргументов.

1)Описать ситуации с дробями:представить в виде пар - (сложение radd, вычитание rsub, умножение rmul, обратная дробь rinv, приведение к несократимой rnorm)
2) map'  - получает функцию и список  элементов и возвращает результат
2.5)zip' получает список x и y и возвращает 1:1:2:2:3:3,  при этом длина - длина самого короткого из них
3)unzip -  из одного списка возвращает пару из двух списков
4)flatten - получает список списков и возвращает один список
5)inverse

Секции, доллары
Секции, доллары


--slit' [] (ls1, ls2) = (ls1, ls2)
--slit' ((x,y):xs) (ls1, ls2) = slit' (xs) (ls1++[x], ls2++[y])

--unzip' list = slit' list ([],[])